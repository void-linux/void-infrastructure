repo3 is currently splinched after we exhausted all the disk space on
kc.voidlinux.us.  The builders remain on the old host though because
we are waiting until the new build infrastructure is online.  Thus in
the meantime we have repo3-build and repo3-mirror hosts that handle
the split.
